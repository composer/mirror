{
    "require": {
        "php": "^8.1",
        "ext-curl": "*",
        "ext-zlib": "*",
        "ext-json": "*",
        "symfony/filesystem": "^6 || ^7",
        "symfony/finder": "^6 || ^7",
        "symfony/lock": "^6 || ^7",
        "symfony/http-client": "^6 || ^7"
    },
    "require-dev": {
        "phpstan/phpstan": "^1.10"
    },
    "scripts": {
        "phpstan": "phpstan"
    }
}
